# ES6 Classes

This project covers ES6 classes, metaprogramming, and symbols.

## Learning Objectives

- How to define a Class
- How to add methods to a class
- Why and how to add a static method to a class
- How to extend a class from another
- Metaprogramming and symbols

## Requirements

- All files are interpreted/compiled on Ubuntu 20.04 LTS using node 20.x.x and npm 9.x.x
- Code is tested using Jest and the command `npm run test`
- Code is verified against lint using ESLint
- Code needs to pass all tests and lint (`npm run full-test`)

## Setup

```
npm install
```

## Tasks

| Task | File | Description |
| --- | --- | --- |
| 0 | `0-classroom.js` | `ClassRoom` class with a `_maxStudentsSize` attribute |
